package DSA_implementation;

//An dictionary is a data structure that stores key-value pairs.
//size(),isEmpty(),elements(),findElem(),findAllELem(),insertItem(),removeElem(),removeAllElem().
//If we search for a key that does not exist, it should return null or an empty list.

public class dictionary {
    
}
